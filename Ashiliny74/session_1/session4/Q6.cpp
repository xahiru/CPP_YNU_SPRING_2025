#include <iostream>
#include <string>

int main() {
    // Dynamically allocate an array of integers
    int n;
    std::cout << "Enter the number of integers: ";
    std::cin >> n;

    // Allocate ⬤