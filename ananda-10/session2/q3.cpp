#include <iostream>
using namespace std;

int main() {
    int l, w;
    cout << "Enter length and width: ";
    cin >> l >> w;
    cout << "Area: " << l * w << "\nPerimeter: " << 2 * (l + w) << endl;
    return 0;
}
